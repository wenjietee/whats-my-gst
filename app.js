// object to store app values
const appValues = {
	// user values
	myAmount: 0,
	myTotal: 0,
	myGstAmount: 0,
	myServiceChargeAmount: 0,
	// gst and service charge rates
	gstRate: undefined,
	serviceCharge: 10,
};

// get gst from data.gov.sg
const ajaxGetGST = () => {};

// calculate gst and service charge or both
const calculateMyServiceCharge = () => {};

const calculateMyGST = () => {};

// remove last value (backspace)
const removeLastValue = () => {};

// start prog
$(() => {});
